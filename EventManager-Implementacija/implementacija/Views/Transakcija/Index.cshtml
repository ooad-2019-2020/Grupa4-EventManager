@model IEnumerable<implementacija.Models.Transakcija>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ukupnoZaUplatu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Korisnik)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NacinPlacanja)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ukupnoZaUplatu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Korisnik.KorisnikId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NacinPlacanja.NacinPlacanjaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TransakcijaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TransakcijaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TransakcijaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
